export enum GenderTypes {
    Male = 1,
    Female,
    TransGender,
    NonBinary,
    PreferNotToDisclose,
}

export enum NotificationTypes {
    SMS,
    Email,
    Whatsapp,
    Call,
}
